import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            {/* Add content for the home page */}
        </div>
    );
};

export default Dashboard;